#
# WARNING: To install Immich, follow our guide: https://docs.immich.app/install/docker-compose
#
# Make sure to use the docker-compose.yml of the current release:
#
# https://github.com/immich-app/immich/releases/latest/download/docker-compose.yml
#
# The compose file on main may not be compatible with the latest release.

name: immich

services:
  immich-server:
    profiles: ["immich", "all"]
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    # extends:
    #   file: hwaccel.transcoding.yml
    #   service: cpu # set to one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl] for accelerated transcoding
    volumes:
      # Do not edit the next line. If you want to change the media storage location on your system, edit the value of UPLOAD_LOCATION in the .env file
      - ${VOLUME_BASE_DIR?:}/immich/data:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      DB_USERNAME: ${IMMICH_DB_USERNAME:?IMMICH_DB_USERNAME not set}
      DB_DATABASE_NAME: ${IMMICH_DB_DATABASE_NAME:?IMMICH_DB_DATABASE_NAME not set}
      DB_PASSWORD: ${IMMICH_DB_PASSWORD:?IMMICH_DB_PASSWORD not set}
    ports:
      - '${IMMICH_PORT}:2283'
    depends_on:
      - redis
      - database
    restart: always
    healthcheck:
      disable: false

  immich-machine-learning:
    profiles: ["immich", "all"]
    container_name: immich_machine_learning
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    # Example tag: ${IMMICH_VERSION:-release}-cuda
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}
    # extends: # uncomment this section for hardware acceleration - see https://docs.immich.app/features/ml-hardware-acceleration
    #   file: hwaccel.ml.yml
    #   service: cpu # set to one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn] for accelerated inference - use the `-wsl` version for WSL2 where applicable
    volumes:
      - model-cache:/cache
    environment:
      DB_USERNAME: ${IMMICH_DB_USERNAME:?IMMICH_DB_USERNAME not set}
      DB_DATABASE_NAME: ${IMMICH_DB_DATABASE_NAME:?IMMICH_DB_DATABASE_NAME not set}
      DB_PASSWORD: ${IMMICH_DB_PASSWORD:?IMMICH_DB_PASSWORD not set}
    restart: always
    healthcheck:
      disable: false

  redis:
    profiles: ["immich", "all"]
    container_name: immich_redis
    image: docker.io/valkey/valkey:9@sha256:546304417feac0874c3dd576e0952c6bb8f06bb4093ea0c9ca303c73cf458f63
    healthcheck:
      test: redis-cli ping || exit 1
    restart: always

  database:
    profiles: ["immich", "all"]
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    environment:
      POSTGRES_USER: ${IMMICH_DB_USERNAME:?IMMICH_DB_USERNAME not set}
      POSTGRES_DB: ${IMMICH_DB_DATABASE_NAME:?IMMICH_DB_DATABASE_NAME not set}
      POSTGRES_PASSWORD: ${IMMICH_DB_PASSWORD:?IMMICH_DB_PASSWORD not set}
      POSTGRES_INITDB_ARGS: '--data-checksums'
      DB_DATA_LOCATION: IMMICH_DB_DATA_LOCATION
      # Uncomment the DB_STORAGE_TYPE: 'HDD' var if your database isn't stored on SSDs
      DB_STORAGE_TYPE: 'HDD'
    volumes:
      # Do not edit the next line. If you want to change the database storage location on your system, edit the value of DB_DATA_LOCATION in the .env file
      - ${IMMICH_DB_DATA_LOCATION}:/var/lib/postgresql/data
    shm_size: 128mb
    restart: always
    healthcheck:
      disable: false

  backup:
    profiles: ["immich", "all"]
    build:
      context: ./backup
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      KEEP_DAILY: ${KEEP_DAILY:-7}
      KEEP_WEEKLY: ${KEEP_WEEKLY:-4}
      KEEP_MONTHLY: ${KEEP_MONTHLY:-12}
      PGHOST: database
      PGPORT: "5432"
      PGDATABASE: ${IMMICH_DB_DATABASE_NAME}
      PGUSER: ${IMMICH_DB_USERNAME}
      PGPASSWORD: ${IMMICH_DB_PASSWORD}
    depends_on:
      - database
    labels:
      com.backup.enabled: "true"
      com.backup.project: "immich"
    volumes:
      - ${VOLUME_BASE_DIR?:}/immich/data:/data/immich-server/data:ro

volumes:
  model-cache:
